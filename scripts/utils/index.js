const IPFSHandler = require('./ipfsHandler');
const { getContractAddress, CONTRACT_ADDRESSES } = require('./contracts');

module.exports = {
    IPFSHandler,
    getContractAddress,
    CONTRACT_ADDRESSES
}; 